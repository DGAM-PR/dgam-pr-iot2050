header:
  version: 14
  includes:
    - repo: meta-iot2050
      file: kas/iot2050.yml

build_system: isar
target: iot2050-image-swu-example

repos:
  meta-iot2050:
    url: https://github.com/siemens/meta-iot2050
    path: meta-iot2050
    branch: master
    commit: 2d304303950933d2480f060c4ae19003c2b71117
    layers:
      meta:
      meta-example: # REQUIRED for iot2050-image-swu-example
      meta-node-red: # REQUIRED for PLC facing IOT2050 Device
      # meta-sm: NOT REQUIRED as only RS232 on X30 port (ASCII Communication) is needed

  meta-dgam-pr:
    path: meta-dgam-pr
    layers:
      .:

local_conf_header:
  root_password: |
    USERS += "root"
    USER_root[password] ??= "root"
    USER_root[flags] ??= "clear-text-password force-passwd-change"
  dgam_customization: |
    # The EIO recipe exists only when the VPN-facing meta-sm layer is enabled.
    # Mask its persistent local bbappend in this non-SM PLC-facing build.
    BBMASK += "meta-dgam-pr/recipes-app/iot2050-eiofsd/"
    # Strip unwanted packages
    IMAGE_INSTALL:remove = "tcf-agent"
    IOT2050_DOCKER_SUPPORT = "0"
    # Extra debug/tooling packages
    IOT2050_DEBIAN_DEBUG_PACKAGES:append = " mosquitto mosquitto-clients"
    # PLC firewall permits DHCP requests; the VPN profile does not.
    FIREWALL_PROFILE = "plc"
    # eno2 static IP: 192.168.1.4/24 (PLC-facing device)
    ENO2_PROFILE = "plc"
    # Custom image packages (dnsmasq-config is PLC-facing only)
    IMAGE_INSTALL:append = " network-config mosquitto-config firewall-config-iot2050 node-red-config serial-config device-identity dnsmasq-config"
    # Node-RED feature
    IOT2050_NODE_RED_SUPPORT = "1"
    IOT2050_META_NODE_RED_PACKAGES = "mraa node-red node-red-gpio node-red-contrib-opcua node-red-contrib-modbus node-red-contrib-s7 node-red-node-serialport"
  package-lock: |
    # 2026-03-23 17:13:35 UTC
    # 18:13:35 in your local Amsterdam time, UTC+1
    ISAR_APT_SNAPSHOT_TIMESTAMP = "1774284815"
    ISAR_USE_APT_SNAPSHOT = "1"